// JavaScript customizado para o site Loki

// Exemplo de inicialização após o carregamento do DOM
document.addEventListener('DOMContentLoaded', () => {
  // Aqui você pode adicionar código JS específico, se necessário
});
